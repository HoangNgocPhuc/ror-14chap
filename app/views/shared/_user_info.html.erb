<%= link_to gravatar_for(current_user, size: Settings.gravatar.size1),
  current_user %>
<h1><%= current_user.name %></h1>
<span>
  <%= link_to t("users.user.view_profile"), current_user %>
</span>
<span>
  <%= pluralize current_user.microposts.size,
    t("microposts.micropost_form.micropost") %>
</span>
