<li>
  <%= gravatar_for user, size: Settings.gravatar.size1 %>
  <%= link_to user.name, user %>
  <% if logged_in? && current_user.admin? && !user.current_user?(current_user) %>
    <%= link_to t("users.user.delete"), user, method: :delete,
      data: {confirm: t(".confirm")} %>
  <% end %>
</li>
